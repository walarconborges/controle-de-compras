document.addEventListener("DOMContentLoaded", function () {
  // Arquivo reservado para comportamentos futuros da homepage.
  // A lógica atual do menu mobile está no próprio homepage.html.
  // Este arquivo permanece ativo para facilitar expansões sem quebrar a página.

  const paginaAtual = document.body;

  if (!paginaAtual) {
    return;
  }

  // Ponto de extensão futuro:
  // adicionar aqui comportamentos específicos da homepage quando necessário.
});
